#include "main.h"

/*
 * shows 1 if the input is a digit value. Another cases, shows 0
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
